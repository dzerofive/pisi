#include <iostream>

#include "vk/pisi_vk_device.hpp"

int main() {
    // Створює пусте вікно, починає луп до поки не закриєш
    // Creating empty window, runs loop until closed
    std::cout << "Starting " << APP_NAME << '\n';

    pisi::gfx::Device device;
}
